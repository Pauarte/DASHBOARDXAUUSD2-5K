Hola! Dues coses per acabar de quadrar-ho tot:

1) EXECUTAR AQUEST SQL A SUPABASE (SQL Editor), és per la taula dels teus
   informes d'anàlisi que encara no existeix:

create table if not exists analysis_reports (
  id bigint generated always as identity primary key,
  bot_id text not null,
  report_date date not null,
  period text not null check (period in ('daily', 'weekly', 'monthly')),
  title text not null,
  content text not null,
  source text not null default 'scheduled-chat',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (bot_id, report_date, period)
);

create index if not exists analysis_reports_bot_date_idx
  on analysis_reports (bot_id, report_date desc);

alter table analysis_reports enable row level security;

create policy "public read analysis_reports" on analysis_reports for select using (true);

2) ACTUALITZAR L'SCRIPT DEL VPS (adjunt sync_mt5_to_supabase.py):
   - He arreglat un bug: les operacions que MT5 tanca amb un magic number
     diferent al d'obertura no es sincronitzaven mai (per això en faltaven
     algunes a l'historial).
   - Atura l'script que tens corrent (Ctrl+C o tanca la finestra).
   - Substitueix "sync_mt5_to_supabase.py" per aquest nou (mateix .env, no
     cal tocar res més).
   - Torna'l a engegar: python sync_mt5_to_supabase.py

Ha de sortir:
   Connected to MT5 account 730432938, syncing every 60s.
   Sync pass OK.

Si surt algun error, envia'm una captura. Gràcies!
